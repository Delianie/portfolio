/* ============================================================
   CONFIG
   Shared feature flags used across the site (slider, menu, …).
============================================================ */

export const PROJECT_LINKS_ENABLED = false;
